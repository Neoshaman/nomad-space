using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class planet : MonoBehaviour {
	
	//terrain is created at the manager level,
	//there is only one terrain, updatined per planet
	//can't be on two planet, nor close to, at the same time
	
	//manager data
	//--terrain size
	
	//planet data
	//--radius in terrain size (hash)
	//--atmosphere height (its radius is derived from planet radius)
	//--proxy mesh
	//--player's holder
	
	//terrain holder
	//--ground data (radius)
	//--initialize the terrain, passing type, position, size
	//--responsible for updating the terrain ground
	
	
	
	
	
	
	//get player()
	
	//get player position()
	
	//get player distance to planet()
	
	//get player distance to ground0()
	
	//get player distance to atmosphere()
	
	//get player geosphere face()
	
	//get player current tiles()
	
	//compute horizon size()




	//generate planet proxy()
	
	//update planet tiling()
	
	//set tile size()
	
	//set tile resolution
	
	//set tile array()
	
	//generate tiles()
	
	//
	
	//tile array coordinate
	
	//tile sphere projected coordinates



	//get global area size()
	
	//set planet radius in area()
	
	//set planet position()



	//https://forums.tigsource.com/index.php?topic=58921.0
	//Cube sphere coordinate

}
